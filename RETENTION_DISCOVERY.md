# Discovery Plan: 몽글 1개월 이후 리텐션 하락

**Date**: 2026-09-21
**Product Stage**: 기존 제품 (출시 후 약 1개월+, 실사용자 있음)
**Discovery Question**: 왜 한 달 시점부터 재방문이 줄고, 무엇을 바꾸면 "매일 하늘을 올려다보는 습관"이 이어지는가?

> 주의: 이 문서의 원인은 **코드/기획 문서 기반 가설**이다. 실제 리텐션 수치·이탈 유저 행동 데이터는 아직 없다 (분석 도구는 Sentry뿐). 실험 E0에서 가장 먼저 검증한다.

---

## 0. 코드 조사에서 확인된 사실

| # | 사실 | 근거 |
|---|------|------|
| F1 | **별도 도감 화면은 없다.** 도감 데이터(`dex_no`, 종별 최고 등급, `getBestFinishBySpecies`)는 있으나 어디에도 렌더링되지 않고, **지도·피드가 사실상 도감 역할**을 한다. 결정: 새 도감 탭은 만들지 않고 피드/지도를 개선한다 | `lib/localStore.ts` "dex screen itself was dropped … not currently rendered anywhere", 탭은 home/map/feed/profile 4개 |
| F2 | 구름 종류 **7종** (일반3/희귀2/전설2). 전설 2종은 날씨 조건 필요 | `server/cloud_species.py`, `lib/cloudSpecies.ts` |
| F3 | Lv.10 캡 = 누적 80회. 하루 1~2회면 약 1~1.5개월에 도달 | `LEVEL_SYSTEM.md` |
| F4 | 푸시/로컬 알림 없음 (`expo-notifications` 미설치) | `package.json` |
| F5 | 분석 도구 없음 (Sentry 크래시만) | `app/_layout.tsx` |
| F6 | streak는 저장값이 아니라 **catch 날짜에서 계산**됨 → 유예/주간목표는 서버 상태 없이 구현 가능 | `localStore.ts` `streaks()` |
| F7 | 피드/지도는 `WHERE account_id = 나`만 조회 → 실제로는 **내 포착 모아보기**이며 타 유저 콘텐츠가 없음 | `server/catches.py` `list_catches`, `list_map_pins` |
| F8 | 그림 그리기 라이브러리 없음 (`react-native-svg`, `@shopify/react-native-skia` 미설치). 또한 **package.json은 Expo SDK 54인데 AGENTS.md는 v57 문서를 읽으라고 함** → 구현 전 버전 확인 필요 | `package.json`, `AGENTS.md` |

## 1. 핵심 가설

**H0**: 몽글은 "수집 게임"을 표방하지만 (a) 수집 현황(진행률·미발견·분류)을 보여주는 곳이 없고 피드/지도는 단순 나열이라 수집 느낌이 약하고, (b) 수집할 대상이 한 달이면 바닥나고, (c) 돌아올 트리거(알림)가 없다. 초기 리텐션은 신선함으로 버텼고, 한 달 시점에 세 가지가 동시에 드러나며 하락한다.

## 2. Ideas Explored

사용자가 선택한 5개 + 직접 추가한 2개. 인프라가 겹치는 것은 묶었다.

| ID | 아이디어 | 출처 |
|----|---------|------|
| **A** | 구름 종류 확장 (7종 → 약 20종): 운형(상/중/하층) + 특수 현상(진주운·야광운·채운 등) | 선택 #1 |
| **B** | 레벨 11+ 및 월간 시즌 (희귀 XP 가중, "9월의 구름" 한정 컬렉션) | 선택 #4 |
| **C** | 알림 시스템 = 푸시(#5) + 30일 이탈자 복귀(#7): 저녁 streak 알림, 오늘의 하늘, D3/D7/D14/D30 복귀 | 선택 #5, #7 |
| **D** | 주간 목표 + streak 유예 ("주 3회", 하루 쉬어도 유지) | 선택 #6 |
| **E** | **모양 구름**: 포착한 구름 위에 몽글한 미색 색연필 브러시(고정)로 모양을 그려 "모양 구름"으로 수집 | 사용자 추가 |
| **F** | **피드/지도의 수집 UI화** (새 도감 페이지 없음): 진행률, 잠긴 슬롯, 폴더링, 검색, 필터를 피드와 지도에 녹임 | 사용자 추가 |
| **G** | (선행) 이벤트 로깅 | 필수 인프라 |

**우선 인사이트**: 피드/지도가 "수집함"처럼 보이지 않으면 종을 늘려도, 모양 구름을 만들어도 모으는 느낌이 안 난다. **F(피드/지도 수집 UI화)가 A, B, E의 전제조건**이라 최우선으로 둔다.

### F. 피드/지도를 수집 UI로 개선 (새 페이지 없음, 탭 4개 유지)

**피드 = "수집 앨범"** (`app/(tabs)/feed.tsx`, F7대로 이미 내 포착만 보여줌)
- **상단 수집 요약**: "구름 도감 3종" + 가로 스크롤 **컬렉션 선반**(발견한 종 카드 + 항상 고정 개수의 "???" 슬롯 2개). **전체 종 수는 의도적으로 숨긴다** — `N/전체`, 진행률 %, "N종 더 찾으면 완성", 잠긴 슬롯 개수 모두 노출 금지. 탭하면 해당 종 폴더로 필터.
- **보기 전환** (기존 `SegmentedControl` 재사용): `전체` / `폴더` / `모양 구름`(E 도입 후).
  - `폴더` 보기: 운형(상층/중층/하층/특수) · 희귀도(일반/희귀/전설) · 월별/장소별 자동 폴더. 폴더 헤더에는 발견한 수만 표기("2종 발견"), 분모 표기 금지.
- **검색·필터**: 종명·메모·장소 검색, 등급(bronze~holo)/희귀도 칩, 정렬(최신/등급/도감번호). 포착 ≥20개일 때 검색창 노출, 그 전엔 진행률·폴더 우선.
- **카드 개선**: 희귀도 별·등급 광택을 카드에 노출, 종별 **"최고 등급 갱신"** 뱃지.
- **획득 순간 연출**: 새 종 발견 시 포착 완료 → 피드의 컬렉션 선반 슬롯이 채워지는 애니메이션(카드 뒤집기).

**지도 = "발견 지도"** (`components/MapScreen*.tsx`)
- 핀을 **희귀도/등급 색**으로 구분, 종별 필터 칩("렌즈구름만 보기").
- **동네 스탬프**: 지역(구/동) 단위로 "이 동네에서 N종 발견" 집계 → "성수동에서 3종 발견" (분모 없이 발견 수만).
- 핀 탭 시 미니 카드(사진·종·등급)에서 바로 피드 상세로.
- 주의: 지도/피드는 F7대로 타 유저 데이터가 없다. **수집 UI는 "내 기록" 기준**으로 설계하고, 사회적 요소는 이번 범위에서 제외한다.

**전체 종 수 비공개 결정 (2026-09-21)**: 종을 추가(A)할 때 "도감이 늘었다"는 서프라이즈를 유지하고, 완성이 가까워 보여 생기는 이탈을 막기 위함. 부작용: "완성" 목표가 사라져 수집 동기를 진행률 대신 **발견 자체의 보상(새 종 카드, 등급 갱신)**에 의존한다. 알려진 잔여 노출: 스토리 카드의 `No.00N` 번호(하한만 노출).

**PRODUCT.md 정합성**: 원칙 #2("희귀도·잠금·진행률이 한눈에 보인다") 중 진행률은 총량 없이 "발견 수"로만 충족한다. 별도 도감 탭 신설은 하지 않는다.

### E. 모양 구름 설계 방향
- **수집 단위 정의 (가장 중요한 결정)**: 자유 그리기만 하면 "수집"이 아니라 낙서가 된다. **하이브리드** 추천 — 그린 뒤 **프리셋 모양 목록**(하트, 토끼, 고래, 강아지, 물고기, 용, 손, 얼굴 등 약 30개)에서 이름을 고르면 도감 슬롯이 채워지고, 직접 이름 지으면 "내 모양 구름" 폴더에 들어간다.
- **검증 불가 원칙**: AI로 "진짜 토끼처럼 생겼는지" 판정하지 않는다 (GPT 비전 정확도 불확실 + 판정 실패 시 좌절). 신고 기반 = 낮은 압박 원칙(PRODUCT.md #4)과 일치. 대신 "이 구름이 그 모양이라고 스스로 인증"하는 플레이.
- **브러시**: 미색(예: `#FFF3D6` 계열) 고정, 색연필 질감(반투명 스트로크 2~3겹 + 약간의 지터/그레인), 끝이 몽글하게 둥근 라운드 캡. 색 선택 UI 없음.
- **가시성 리스크**: 미색 선은 흰 구름·밝은 하늘 위에서 안 보인다. 미색은 유지하되 **아주 옅은 청회색 외곽 그림자(halo)** 로 대비 확보 필요 (E5 실험에서 검증).
- **결과물**: 그림이 합성된 이미지가 곧 공유용 스토리 카드 소재 → 원칙 #3("모든 포착은 공유할 결과물")과 결합.
- **기술 전제**: Skia(색연필 질감에 적합) 또는 react-native-svg. **네이티브 모듈 추가 → 새 dev-client/EAS 빌드 필요**, 웹 배포는 CanvasKit 로딩 부담. 구현 전 Expo 버전(F8) 확인.

### A. 종 확장 방향
- 후보(운형 기반, 1차 +8~10종): 층적운, 고층운, 난층운, 고적운 변형, 권층운(햇무리), 진주운, 야광운, 채운(무지개구름), 아치운/롤구름 등.
- **위험**: 종이 늘수록 GPT 분류 정확도가 떨어지고 (오분류 = 등급/희귀도 불신), 날씨상 못 잡는 종이 많으면 "안 나와서 지루함". 각 주에 **잡을 수 있는 종이 최소 1~2개**는 되도록 일반/희귀 비율 설계.

### B. 시즌/레벨 방향
- Lv.11~20 곡선 연장 + MAX 배지, 희귀/전설 XP 가중치(LEVEL_SYSTEM 미해결 질문 2개 해소).
- 월간 시즌은 콘텐츠 운영 부담이 있어 **A/F가 검증된 뒤로 미룬다.**

### C. 알림 방향 (푸시 + 복귀)
- **1단계 = 로컬 알림 (서버 불필요)**: 앱 열 때마다 재예약. 저녁 "오늘 아직 하늘 안 봤어요"(streak 위험 시), 마지막 접속 D3/D7/D14/D30 복귀 문구.
- **2단계 = 서버 푸시**: 푸시 토큰 테이블 + 스케줄러(서버가 서버리스이므로 cron 필요). "오늘의 하늘 확률 ↑" 종류 알림은 날씨→종 매핑 정확도가 낮으니 "~일 수도 있어요" 톤 유지.
- **규제 확인 필요**: 한국 정보통신망법상 광고성 푸시는 별도 동의 및 야간(21~08시) 제한 이력이 있음. streak 안내는 정보성이지만 **복귀 유도 문구는 광고성으로 분류될 수 있어 검토 필요.**

### D. 주간 목표/유예 방향
- streak가 날짜에서 계산되는 구조(F6)라, **"1일 공백 허용"** 규칙은 클라이언트 로직만으로 가능. 저장 상태가 필요한 "프리즈 아이템"은 나중에.
- 카피는 압박 대신 "이번 주 2/3 · 하루 쉬어도 괜찮아요".

## 3. Selected Ideas for Validation

| 순위 | 아이디어 | 이유 |
|------|---------|------|
| 1 | **G 이벤트 로깅 + E0 데이터 분석** | 나머지 판단의 근거. 비용 최소 |
| 2 | **F 피드/지도 수집 UI화(진행률·폴더·검색)** | 핵심 약속(수집)이 UI에 드러나지 않음. A·B·E의 전제. 새 탭 없이 기존 화면 개선 |
| 3 | **C 로컬 알림 (streak + 복귀)** | 서버 없이 재방문 트리거 확보, 비용 낮음 |
| 4 | **D 유예/주간 목표** | 클라 로직만으로 가능. streak 끊김 이탈 방지 |
| 5 | **E 모양 구름** | 차별화 크고 공유 유발. 단 가장 비싸고 불확실 → fake door로 수요 먼저 |
| 6 | **A 종 확장** | 수집 UI 개선 이후 의미. 분류 정확도 평가 후 |
| 7 | B 레벨11+/시즌 | A1 확인 후 |

## 4. Critical Assumptions

Impact / Uncertainty: H·M·L

| # | Assumption | Category | Impact | Uncertainty | Priority |
|---|-----------|----------|--------|-------------|----------|
| A1 | 이탈 유저는 **더 모을 게 없어서**(종 수집 완료 근접/Lv 9~10) 떠났다 | Value | H | H | **1 (leap of faith)** |
| A2 | 피드/지도에 진행률·잠금 실루엣을 넣으면 주간 포착 횟수가 늘어난다 | Value | H | M | **2** |
| A3 | 사용자는 로컬 알림/복귀 알림을 허용하고 반응한다 (스팸으로 느끼지 않는다) | Value | H | H | **3 (leap of faith)** |
| A4 | 사용자는 포착 사진 위에 그림 그리기를 재밌어 하고 실제로 한다 | Value | H | H | **4 (leap of faith)** |
| A5 | 미색 브러시가 밝은 하늘/흰 구름 위에서 충분히 보인다 | Usability | M | H | 5 (반나절 프로토타입으로 검증 가능) |
| A6 | GPT가 20종 이상을 실용 수준으로 분류한다 (특히 신규 희귀종) | Feasibility | H | H | **6** |
| A7 | 종을 늘려도 날씨상 못 잡는 종이 많아 "안 나오는 지루함"이 생기지 않는다 | Value | M | H | 7 |
| A8 | 주간 목표/1일 유예가 streak 끊김 후 복귀율을 높인다 | Value | M | M | 8 |
| A9 | Skia(또는 SVG)로 색연필 질감을 모바일+웹에서 60fps로 구현 가능 | Feasibility | M | H | 9 |
| A10 | 프리셋 모양 목록 + 직접 이름짓기 하이브리드가 자유 그리기보다 수집욕을 자극한다 | Usability | M | M | 10 |
| A11 | 검색/폴더는 포착 수가 충분한 유저에게만 가치가 있다 (신규엔 진행률이 우선) | Usability | L | M | 11 |
| A12 | 복귀 알림 문구가 정보통신망법상 광고성으로 분류돼도 운영 가능하다 | Viability | M | H | 12 (법률 확인) |
| A13 | 1인 개발 기준으로 월간 시즌 콘텐츠를 지속 공급할 수 있다 | Viability | M | M | 13 |
| A14 | 유저 수가 A/B 테스트가 가능할 만큼 되는가 (표본 부족 시 전후 비교로 대체) | Viability | M | H | 14 |

## 5. Validation Experiments

| # | 검증 가정 | 방법 | 성공 기준 | 노력 | 기간 |
|---|----------|------|----------|------|------|
| **E0** | A1, A14 | Supabase SQL로 이탈 유저 분석 + 이벤트 로깅 도입 | 이탈 유저 중 **≥40%가 6종 이상 또는 Lv.9 이상**이면 A1 확정 | S | Day 1~2 |
| **E1** | A2, A11 | 피드/지도 수집 UI v1(진행률·컬렉션 선반·폴더) 출시 후 전/후 비교 | 피드 재방문율 +30%, 주간 포착 수 +20% | M | Wk 1~2 |
| **E2** | A3, A12 | 로컬 알림 (저녁 streak + D3/D7/D14 복귀), **50% 홀드아웃** | 허용률 ≥50%, 알림→앱 오픈 ≥10%, D30 복귀율 홀드아웃 대비 +5%p | S | Wk 1~3 |
| **E3** | A4, A10 | **Fake door**: 포착 완료 화면에 "✏️ 구름에 그려보기" 버튼 → "곧 만나요" 시트 + 클릭 수집. 병행: 5명에게 스크린샷에 그리게 하는 컨시어지 | 포착 완료 중 **탭률 ≥15%**, 5명 중 ≥3명이 프롬프트 없이 그림 | S | Wk 1~2 |
| **E4** | A6, A7 | 라벨링된 사진 40~100장으로 확장 프롬프트 오프라인 평가 | 일반종 top-1 ≥85%, 신규 희귀종 ≥60%, 오분류가 상위 등급으로 쏠리지 않음 | M | Wk 2 |
| **E5** | A5, A9 | 브러시 프로토타입 (Skia 한 화면) + 밝은/흐린/노을 사진 10장에 그려 가시성 확인 | 10장 중 ≥8장에서 선이 식별 가능(외곽 halo 포함), 실기기 ≥55fps | M | Wk 2~3 |
| **E6** | A8 | 1일 유예 + "주 N회" 문구 도입 후, streak 끊긴 유저의 3일 내 복귀율 비교 | 복귀율 +10%p | S | Wk 3 |

## 6. Experiment Details

**E0. 이탈 유저 데이터 분석 (SQL, 즉시 가능)**
- 가설: 이탈 유저는 대부분 종 수집 완료/레벨 상한 근처에서 멈췄다.
- 대상: `account_catches`(로그인 유저만; 비로그인 로컬 유저는 미포함이라 편향 주의).
- 쿼리 개요:
  ```sql
  WITH u AS (
    SELECT account_id,
           COUNT(*)                      AS total_catches,
           COUNT(DISTINCT cloud_name)    AS species,
           MIN(captured_at)              AS first_at,
           MAX(captured_at)              AS last_at,
           COUNT(DISTINCT captured_at::date) AS active_days
    FROM account_catches GROUP BY account_id
  )
  SELECT
    (now() - last_at > interval '14 days') AS churned,
    COUNT(*)                                AS users,
    AVG(total_catches) AS avg_catches, AVG(species) AS avg_species,
    AVG(active_days)   AS avg_days,
    AVG(EXTRACT(EPOCH FROM last_at - first_at)/86400) AS avg_lifetime_days,
    COUNT(*) FILTER (WHERE species >= 6)      AS near_complete,
    COUNT(*) FILTER (WHERE total_catches >= 63) AS near_lv10
  FROM u GROUP BY 1;
  ```
- 함께 볼 것: 주차별 코호트 리텐션(가입 주 대비 W1/W2/W4/W6), 마지막 포착 종 분포.
- 이벤트 로깅(도입 목록): `app_open`, `capture_start`, `capture_success`(종·등급), `dex_view`, `dex_search`, `notif_permission`, `notif_open`, `streak_broken`, `streak_saved_by_grace`, `shape_door_tap`.
- 결정: A1 확정이면 → A/F/B 우선. 아니면 → 알림(C)·streak(D)·온보딩 쪽 원인 재탐색 + 유저 인터뷰.

**E1. 피드/지도 수집 UI v1**
- 셋업: 피드 상단에 진행률 + 컬렉션 선반 + 보기 전환(전체/폴더), 종 필터 칩. 지도는 핀 색 구분 + 종 필터 + 동네 집계. 검색은 포착 ≥20개일 때 노출. **새 탭/페이지 없음.**
- 측정: 피드·지도 탭 재방문율, 컬렉션 선반/폴더 탭 수, 전후 주간 포착 수, D14 리텐션.
- 성공 시 → A(종 확장) 착수. 실패 시 → "수집 동기" 자체가 약한 것이므로 인터뷰로 전환.

**E2. 로컬 알림**
- 셋업: `expo-notifications` 로컬 예약. 앱 오픈마다 취소 후 재예약. (a) 저녁 19시대 streak 위험 알림(당일 미포착일 때만), (b) 마지막 오픈 후 D3/D7/D14/D30 복귀 문구. 홀드아웃 50%는 알림 없음.
- 측정: 허용률, 알림 탭→오픈 전환, 알림군 vs 홀드아웃 D30.
- 실패 시 → 문구/시간 조정 1회 후에도 미개선이면 서버 푸시 투자 보류.
- 선행: 알림 권한 요청은 반드시 InteractionManager 이후에 (메모리: 마운트 시 네이티브 부작용으로 iOS 크래시 이력).

**E3. 모양 구름 Fake door**
- 셋업: 포착 완료 후 버튼 1개 + "준비 중" 시트 + 이메일/알림 신청 여부. 병행 컨시어지: 사진 5장 스크린샷 + 그림 앱으로 그려서 반응 확인.
- 측정: 노출 대비 탭률, 신청률, 컨시어지 참여자의 자발적 공유 여부.
- 결정: 탭률 ≥15% → E5(프로토타입)→구현. <5% → 폐기 또는 피드 내 "모양 태그"(그리기 없이 텍스트 태그)로 축소.

**E4. 종 확장 분류 평가**
- 셋업: 앱에서 실제 촬영된 사진 + 웹 수집 사진에 정답 라벨, 확장 프롬프트로 `/api/recognize` 배치 호출. 혼동 행렬 작성.
- 결정: 기준 미달 종은 출시 제외 or "AI가 확신하지 못했어요 → 직접 선택" UX 추가.

**E5. 브러시 프로토타입**
- 셋업: 한 화면에서 사진 위 Skia 스트로크 (미색, 반투명 다겹, 라운드 캡, 옅은 halo). 밝은 하늘/흐린 하늘/노을/야간 10장에서 식별성 확인, 실기기 fps 측정.
- 결정: 웹 성능/번들 문제가 크면 웹은 "모바일에서 그리기 가능" 안내로 제한.

**E6. 유예/주간 목표**
- 셋업: `streaks()`에 "1일 공백 허용" 옵션 + UI 카피 변경. 전후 비교(표본 부족 시 인터뷰 병행).
- 측정: streak 끊김 후 3일 내 재포착 비율.

## 7. Discovery Timeline

- **Week 1**: E0(분석+로깅) → E1 착수(피드/지도 수집 UI v1), E2 착수(로컬 알림), E3 fake door 배포
- **Week 2**: E1 출시·측정, E4(분류 평가), E5(브러시 프로토타입), E3 컨시어지
- **Week 3**: E6 배포, 전 실험 결과 정리 → 의사결정 회의 (A, B, E 본개발 여부)

## 8. Decision Framework

- E0에서 A1 확정 (이탈자 ≥40% 종 수집/레벨 상한 근접) → 수집 UI(F) + 종 확장(A) + 레벨11+(B)에 집중, 알림은 2순위
- E0에서 A1 기각 → 유저 인터뷰로 원인 재탐색, 알림(C)·streak(D)·온보딩 우선
- E1 성공 (주간 포착 +20%, 피드 재방문 +30%) → 종 확장 A 착수 / 실패 → 인터뷰
- E2 성공 (홀드아웃 대비 D30 +5%p) → 서버 푸시(오늘의 하늘) 투자 / 실패 → 알림 확장 중단
- E3 탭률 ≥15% → E5 통과 시 모양 구름 본개발 / <5% → 폐기 또는 텍스트 태그로 축소
- E4 미달 종 → 출시 제외 or 수동 선택 UX
- E6 성공 → 주간 목표 UX 정식화, 서버 프리즈 아이템은 이후

## 9. 구현 전 확인할 것

1. Expo SDK 버전: `package.json`은 54, `AGENTS.md`는 v57 문서를 요구 → 어느 쪽이 맞는지 정리 후 문서 확인.
2. 네이티브 의존성(expo-notifications, Skia) 추가 → 새 EAS 빌드 필요, 최근 iOS 크래시 이력이 있으므로 TestFlight로 먼저 확인.
3. 서버(FastAPI, 서버리스)에 푸시 토큰/스케줄러를 붙일 방법 (2단계 이후).
4. 광고성 알림 법적 요건 확인 (A12).

## 10. 구현 순서

원칙: ① 측정 먼저 ② JS만 바꾸는 것(네이티브 재빌드 불필요)을 먼저 묶어 출시 ③ 네이티브 의존성(expo-notifications, Skia)은 **한 번의 EAS 빌드로 묶어** TestFlight 검증 횟수를 줄인다 ④ 앞 단계 결과가 뒷 단계의 착수 조건.

| 순서 | 작업 | 종류 | 크기 | 선행 | 핵심 파일 |
|-----:|------|------|:---:|------|----------|
| 0 | Expo 버전 정리 (SDK 54 vs AGENTS.md v57) + 해당 버전 문서 확인 | 준비 | S | - | `package.json`, `AGENTS.md` |
| 1 | E0 이탈 유저 SQL 분석 (코드 없음) | 분석 | S | - | Supabase `account_catches` |
| 2 | 이벤트 로깅: `POST /api/events` + `lib/analytics.ts` (자체 테이블, 네이티브 의존성 없음) | JS+서버 | S | - | `server/main.py`, `lib/analytics.ts`, `lib/api.ts` |
| 3 | **피드 수집 요약**: 발견 수 + 컬렉션 선반(발견 종 + 고정 "???" 슬롯). 데이터는 `getBestFinishBySpecies` + `KNOWN_CLOUDS` 재사용 | JS | S | 2 | `app/(tabs)/feed.tsx`, `lib/cloudSpecies.ts` |
| 4 | **피드 폴더/필터/검색**: `SegmentedControl`(전체/폴더), 종·희귀도·등급 칩, 검색(≥20개일 때 노출) | JS | M | 3 | `feed.tsx`, `components/SegmentedControl.tsx` |
| 5 | **모양 구름 fake door** 버튼 (포착 완료 화면) + 탭 이벤트 | JS | S | 2 | `app/capture.tsx` |
| 6 | **지도 발견 UI**: 희귀도 색 핀, 종 필터, 동네 스탬프 | JS | M | 3 | `components/MapScreen.tsx`, `MapScreen.native.tsx` |
| 7 | **streak 1일 유예 + 주 N회 목표** (`streaks()` 수정, 홈/프로필 카피) | JS | S | 2 | `lib/localStore.ts`, `home.tsx`, `profile.tsx` |
| — | **▶ 릴리스 R1** (2~7, JS만). 이후 1~2주 데이터 수집, E1/E3/E6 판정 | | | | |
| 8 | **로컬 알림**: `expo-notifications`, 앱 오픈 시 재예약, 홀드아웃 50%. 권한 요청은 `InteractionManager` 이후 | 네이티브 | M | 7 (유예 규칙과 알림 문구가 맞물림) | `app/_layout.tsx`, `lib/notifications.ts` |
| 9 | 종 확장 분류 평가 (E4): 라벨된 사진으로 확장 프롬프트 오프라인 테스트 | 분석 | M | 3 | `server/main.py` `/api/recognize` |
| — | **▶ 릴리스 R2** (8, 네이티브 빌드 1회). TestFlight 먼저 | | | | |
| 10 | **종 확장** (통과한 종만): 서버/클라 종 목록 동시 수정. **`dexNo`가 배열 인덱스 기반이므로 기존 종 순서를 바꾸지 말고 끝에만 추가** | JS+서버 | M | 9, 3 | `server/cloud_species.py`, `lib/cloudSpecies.ts` |
| 11 | **레벨 11+ / MAX 배지 / 희귀 XP 가중** | JS(+서버) | S | 1의 A1 확정 | `lib/levelSystem.ts`, `LEVEL_SYSTEM.md` |
| — | **▶ 릴리스 R3** (10~11) | | | | |
| 12 | E5 브러시 프로토타입 (Skia 한 화면, 실기기 fps·가시성) | 네이티브 | M | 5의 탭률 ≥15% | 신규 화면 |
| 13 | **모양 구름 본개발**: 데이터 모델(`shape_name`, 획/합성 이미지), 프리셋 30종, 피드 "모양 구름" 보기, 공유 카드 합성 | 네이티브+서버 | L | 12 통과 | `capture.tsx`, `share.tsx`, `server/catches.py` |
| — | **▶ 릴리스 R4** (12~13, 네이티브 빌드) | | | | |
| 14 | 서버 푸시("오늘의 하늘"): 토큰 테이블 + cron | 서버 | L | 8의 E2 성공 | `server/` |
| 15 | 월간 시즌 | JS+서버 | L | 10, 11 검증 후 | - |

### 순서의 이유
- **2 → 3**: 로깅 없이 수집 UI를 내면 효과를 못 잰다.
- **3~7을 R1로 묶음**: 전부 JS라 네이티브 빌드 없이 가장 빨리 나가고, 잡음이 적어 효과 판정이 쉽다.
- **7 → 8**: "하루 쉬어도 유지" 규칙이 정해져야 "streak 위험" 알림 조건과 문구를 정할 수 있다.
- **8을 별도 릴리스**: 최근 iOS 크래시 이력이 있고 네이티브 모듈 추가라 R1과 섞으면 원인 분리가 어렵다.
- **10은 9 뒤**: 오분류 나는 종을 먼저 내면 등급/희귀도 신뢰가 깨진다.
- **12~13은 마지막**: 가장 비싸고 불확실하며, 5의 fake door 탭률이 근거.

### 중간 판단 지점
- 1 결과가 A1 기각이면 → 10, 11, 15는 보류하고 인터뷰 후 재계획.
- R1 후 E1 실패면 → 8 이후로 넘어가기 전에 수집 동기 인터뷰.

## 11. 공유 전략 결정 (2026-09-21)

**목표**: 불특정 다수(인스타 스토리 등)에게 공유돼 마케팅 효과를 얻는다. 앱 내 소셜은 만들지 않는다.
**주 루프**: Viral / Usage (포착 → 카드 → 외부 공유 → 유입). 2순위 User-Generated(모양 구름 챌린지), 3순위 Referral(프레임/뱃지 보상).

**불특정 다수 타깃이므로 우선순위**: 링크보다 **카드 이미지 자체의 브랜딩/CTA**가 1순위. 카드 한 장이 광고판이어야 한다.

현재 공유 흐름의 누수 (코드 기준, 실행 검증은 안 함):
- 카드에 앱 이름과 "나도 해보기" 유도 없음 (마스코트만 있음)
- `mongle.expo.app/share?catchId=` 공개 조회 API/페이지 없음, 딥링크 설정 없음 → 링크 수신자는 카드를 못 볼 가능성
- 공유 진입점이 포착 직후와 피드 카드 두 곳뿐

다음 단계 (제안, 미구현):
1. 카드에 워드마크 + "나도 구름 도감 채우기" + QR/짧은 코드
2. 공유 트리거 확대: 희귀/전설/홀로 획득, 새 종 발견, 연속 관측 마일스톤, 월간 리캡
3. 카드 꾸미기(프레임/스티커), "내 구름 지도" 공유 카드
4. (결정 대기) 공개 공유 페이지 — 사진/대략적 위치 공개 동의 필요

## 12. 지도 개편 A안 (구현됨)

풀스크린 지도 + 바텀시트. 핀은 구름 아이콘 + 희귀도 링, 가까운 기록은 클러스터로 묶임, 시트에서 항목을 고르면 지도가 해당 핀으로 이동. 웹은 Leaflet(OSM 타일을 채도 낮춰 사용), 네이티브는 react-native-maps.
